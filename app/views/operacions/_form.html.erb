<%= form_with(model: operacion, local: true) do |form| %>
  <% if operacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operacion.errors.count, "error") %> prohibited this operacion from being saved:</h2>

      <ul>
      <% operacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :operacion_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :operacion_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
